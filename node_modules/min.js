let minimum = function (array){
	this.array = array;
	let min = array[0];
  	let max = min;
  	for (let i = 1; i < array.length; ++i) {
      if (array[i] > max) max = array[i];
      if (array[i] < min) min = array[i];
  }
}